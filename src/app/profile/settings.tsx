import { PlaceholderScreen } from "@/components/nav/placeholder-screen";

export default function SettingsScreen() {
	return (
		<PlaceholderScreen
			description="Email, phone, password and notification settings are next up."
			title="Settings"
		/>
	);
}
