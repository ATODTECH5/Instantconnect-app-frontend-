import { PlaceholderScreen } from "@/components/nav/placeholder-screen";

export default function KycScreen() {
	return (
		<PlaceholderScreen
			description="Identity checks land here once the verification flow is built."
			title="KYC Verification"
		/>
	);
}
